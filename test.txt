#!/bin/bash

echo current user "$(whoami)"
echo current working directory is "$(pwd)"
echo all the files inside the current direcotory "$(ls -la)"
echo today is "$(date)"
echo check information about the RAM usage "$(free -m)"
echo test Jenkins-2, merge dupa restart?
